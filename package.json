{
  "name": "etsa-public-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "championship:sync": "node scripts/sync-championship.mjs",
    "championship:validate": "node scripts/validate-championship.mjs",
    "test": "node --test tests/*.test.mjs",
    "site:check": "node scripts/check-site.mjs",
    "check": "npm test && npm run championship:validate && npm run site:check"
  },
  "engines": {
    "node": ">=20"
  }
}
